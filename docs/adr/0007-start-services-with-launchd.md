# Start the gallery with launchd

The project provides a single Bun start command for both loopback services and a macOS launchd configuration that runs it at login. Tailscale Serve retains the separate private HTTPS routes, so the gallery is available on the owner's devices whenever the laptop is running.
